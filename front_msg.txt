Confirmado. 100% backend. Mirá la respuesta:

"pmin": null,
"pmax": null

Dos bugs del backend en esos campos:

Bug 1 — nombre mal. Devuelve pmin/pmax (todo minúscula). El contrato (API-FRONTEND.md:312) y el POST usan pMin/pMax (camelCase). Tu front lee grupo.pMin → no existe pMin, existe pmin → undefined. Clásico bug Java: campo pMin con getter getPMin() que Jackson serializa raro.

Bug 2 — valor null. Aunque arreglen el nombre, igual vienen null. O sea el backend no los persiste/no los devuelve. Fijate: potenciaMedia: 55.0 SÍ está, y el precio se calculó (610). Guardaron la media y calcularon precio, pero pmin/pmax crudos vuelven null.

Por eso editar muestra vacío: el front lee null → input vacío. No es problema de guardado tuyo, es lectura rota.

Mensaje para el backend:
ar
▎ En GET /grupos-electrogenos, el GrupoElectrogenoResponseDTO tiene dos problemas con la potencia:
▎ 1. Los campos salen como pmin/pmax (minúscula), pero el contrato y el POST usan pMin/pMax (camelCase). Es un problema de naming del getter/Jackson — probablemente el getter es getPMin() y serializa mal. Anótenlos con @JsonProperty("pMin") / @JsonProperty("pMax") o arreglen el getter.
▎ 2. Aunque corrijan el nombre, los valores vuelven null. El potenciaMedia y el precioVentaCalculado sí se calculan, así que los datos existieron al crear — falta persistir o exponer pMin/pMax crudos en la entidad/DTO.

Pediles que prueben el GET después de arreglar: tienen que venir "pMin": 50.0, "pMax": 60.0 (o lo que cargaste).